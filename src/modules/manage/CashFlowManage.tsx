import CashFlowManagePanel from '@/pages/manage/CashFlowManagePanel'

/* Manage & Adjust module — the data-management half of the Treasury Workspace
 * (segregated from the reports/viewing "Cash Flow" module). Home for Import
 * runs, Cycles & versions, Adjustments, and Labels & mappings. */
export default function CashFlowManage() {
  return <CashFlowManagePanel />
}
